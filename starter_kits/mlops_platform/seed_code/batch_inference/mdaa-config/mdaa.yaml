organization: "{{env_var:MDAA_ORG}}"

domains:
  mlops:
    environments:
      dev:
        account: default
        modules:
          batch-pipeline:
            module_path: "@aws-mdaa/sagemaker-pipeline"
            module_configs:
              - ./pipeline.yaml
